mkdir -p ${WORK_DIR}/05-jointcalCoadd
cd ${WORK_DIR}/05-jointcalCoadd
ln -sfn ${WORK_DIR} pardir
cp -n $RTF/config/jointcalCoaddConfig.py .
build_visit_lists.py -i pardir/input -l pardir/02-processCcd/log/*/*.log --idopt selectId
cp pardir/03-makeDiscreteSkyMap/patches* .
run_jointcalCoadd.py -c jointcalCoaddConfig.py --fromslac
pipelineSet WORK_DIR ${WORK_DIR}